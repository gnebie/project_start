import argparse

description ='Stable diffusion python launcher (not unicorn api compatible).'
version= "0.0.2"

# parser = argparse.ArgumentParser(description=description)
# parser.add_argument('-V', '--version', action="version", version="%(prog)s " + version)

# # log arguments 
# parser.add_argument('-v', '--verbose', action="count", default=0, help="verbose level, repeat up to three times.1-info 2-debug 3-trace")
# parser.add_argument('-q', '--quiet', action="count", help="quiet level, repeat up to 2 times, 1-error 2-critical.")
# parser.add_argument('--log-file', help='Specify a log-filename.')
# parser.add_argument('--log-folder', help='Specify the log folder.')
# parser.add_argument('-l', '--log-level', help='Specify a log file level.', type=str, choices=['TRACE','DEBUG','INFO','WARNING','ERROR','CRITIQUE'])

# # run arguments
# parser.add_argument('--dry-run', action='store_true', help='Make a code simulation without call the changing methods.')
# parser.add_argument('--ignore-errors', action='store_true', help='Continue the script if there is an error.')

# parser.add_argument('--interactive', action='store_true', help='Interactive api console.')
# parser.add_argument('--config-file', help='Configuration file path, defalut "./contiguration.json".')


# # api arguments
# parser.add_argument('--file-name', help='Specify the output file name.')
# parser.add_argument('--overwrite', help='Specify the api right to overwrite the files')
# parser.add_argument('--format', help='Specify the output file format.')
# parser.add_argument('--limit', help='Specify the result limit number.')
# parser.add_argument('--timeout', help='Specify the call timeout.')

# # api arg gestion
# parser.add_argument('--proxy-host', help='Specify the api-host.')
# parser.add_argument('--proxy-port', help='Specify the api-port.')
# parser.add_argument('--headers', help='Specify the api header.')
# parser.add_argument('--ssl-cert', help='Specify the api cert')
# parser.add_argument('--ssl-key', help='Specify the api key')
# parser.add_argument('--username', help='Specify the api username')
# parser.add_argument('--password', help='Specify the api password')
# parser.add_argument('--token', help='Specify the api token')
# parser.add_argument('--profile', help='Specify the api profile')
# parser.add_argument('--environment', help='Specify the api environment')
# parser.add_argument('--language', help='Specify the api language')
# parser.add_argument('--locale', help='Specify the api locale')


# parser.add_argument('--sslcert', help='')

# print(args.accumulate(args.integers))
